{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal permissions for the bundled Cross Bus Explorer UI",
  "windows": ["main"],
  "permissions": ["core:default"]
}
